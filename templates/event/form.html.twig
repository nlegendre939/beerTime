{% extends 'base.html.twig' %}

{% block title %}
    {{ isNew ? 'Créer' : 'Modifier' }} un événement - {{ parent() }}
{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/components/form.css') }}">
{% endblock %}

{% block body %}
    {# 
        Pour afficher les champs un par un, 2 fonctions : 
        form_start => affiche la balise ouvrante du formulaire et la configuration nécessaire
        form_end => affiche la balise de fermeture du formulaire et éventuellement les champs non affichés  
    #}
    {{form_start(form)}}

    <div class="form-row">
        {{form_row(form.name)}} {# form.name nom lié au nom EventType lui-même lié au nom champ repository #}
    </div>
    <h2>Média</h2>
    <div class="form-row">
        {{ form_row(form.pictureFile) }}
        {{ form_row(form.pictureUrl) }}
    </div>
    <h2>Dates</h2>
    <div class="form-row">
        {{ form_row(form.startAt) }}
        {{ form_row(form.endAt) }}
    </div>
    <h2>Détails</h2>
    <div class="form-row">
        {{ form_row(form.price) }}
        {{ form_row(form.capacity) }}
    </div>
    <h2>Lieu</h2>
    <div class="form-row">
        {{ form_row(form.place) }}
    </div>
    <div class="form-row">
        {{ form_row(form.description) }}
        {{ form_row(form.rules) }}
    </div>

    <div class="form-end">
        {{ form_row(form.submit) }}
    </div>
    {{ form_end(form) }}
{% endblock %}